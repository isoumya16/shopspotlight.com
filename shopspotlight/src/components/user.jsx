import React from 'react';

const User = () => {
    return (
        <div>
            <h2>User Dashboard</h2>
            <p>You can view products but cannot add or delete them.</p>
        </div>
    );
};

export default User;
